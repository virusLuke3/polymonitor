# Production Nginx exposes a verified same-origin Range proxy for the ranked
# Protomaps archive. The relative URL keeps CORS out of the render path while
# the OpenFreeMap and local-country fallbacks remain available on failure.
VITE_PMTILES_URL=/map-tiles/planet.pmtiles
